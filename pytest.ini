[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
addopts =
    --cov=src
    --cov=scripts
    --cov-report=term-missing
    --cov-fail-under=40
    --cov-config=.coveragerc
