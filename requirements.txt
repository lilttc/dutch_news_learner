# Dutch News Learner - Dependencies
# Install: pip install -r requirements.txt

# Data Ingestion
youtube-transcript-api>=0.6.2
google-api-python-client>=2.0.0
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0  # Postgres driver (Neon)

# NLP (Phase 2)
spacy>=3.7.0

# LLM (translation)
openai>=1.0.0

# Search (Related Reading)
ddgs>=9.0.0

# Auth (Phase 6F)
# Werkzeug: PBKDF2 password hashes (avoids bcrypt/passlib env conflicts)
werkzeug>=3.0.0
python-jose[cryptography]>=3.3.0

# API & Frontend (Phase 3)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
slowapi>=0.1.9
streamlit>=1.37.0
